# Internal repository with workflow for the nmd-transcriptome project

## Map
- `workflow/` for snakemake workflow and dependecies
- `data/` intermediate files used in computation
- `config/` for metadata and files
